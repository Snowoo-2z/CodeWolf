import {createClient} from 'https://esm.sh/@supabase/supabase-js@2';
const config=window.CODEWOLF_CONFIG||{};
export const supabase=config.SUPABASE_URL&&config.SUPABASE_ANON_KEY?createClient(config.SUPABASE_URL,config.SUPABASE_ANON_KEY):null;
window.CodeWolfAuth={async loginWithGitHub(){if(!supabase){alert('Supabase doit être configuré.');return}const {error}=await supabase.auth.signInWithOAuth({provider:'github',options:{redirectTo:location.origin+'/auth-callback'}});if(error)alert('Connexion impossible : '+error.message)},async logout(){if(supabase)await supabase.auth.signOut();location.href='/'},async getUserSession(){if(!supabase)return null;const {data}=await supabase.auth.getSession();return data.session?.user||null},async requireAuth(){const user=await this.getUserSession();if(!user)location.href='/signin';return user}};